<h1><%= @list.name %></h1>
<%= link_to 'Edit list name', edit_list_path(@list) %>
<%= button_to 'Delete list', list_path, method: :delete %>

<ul>
  <% @list.items.each do |item| %>
    <li><%= item.title %></li>
  <% end %>
</ul>